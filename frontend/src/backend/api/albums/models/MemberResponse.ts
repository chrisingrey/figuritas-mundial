export interface MemberResponse {
  id: string;
  albumId: string;
  userId: string;
  roleId: string;
  status: string;
  joinedAt: string;
  email: string;
  username?: string;
  fullname?: string;
  surname?: string;
}
